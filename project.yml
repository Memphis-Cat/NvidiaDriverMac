name: NvidiaDriverMac

options:
  bundleIdPrefix: com.memphiscat
  deploymentTarget:
    macOS: "26.0"
  groupSortPosition: top
  generateEmptyDirectories: true

settings:
  base:
    CLANG_CXX_LANGUAGE_STANDARD: "c++20"
    CLANG_CXX_LIBRARY: "libc++"
    SWIFT_VERSION: "5.0"
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: NO

targets:
  RTXMac:
    type: driver-extension
    platform: macOS
    scheme: {}
    sources:
      - path: macos/Driver
        excludes:
          - README.md
      - path: src/core/sysmem_flush.cpp
      - path: src/core/falcon_policy.cpp
      - path: src/core/boot_preflight.cpp
      - path: src/core/boot_recovery.cpp
      - path: src/core/boot_sequence_policy.cpp
      - path: src/core/boot_orchestrator.cpp
      - path: src/core/boot_package.cpp
      - path: src/core/boot_package_policy.cpp
      - path: src/core/package_dma_plan.cpp
      - path: src/core/sha256.cpp
      - path: src/core/pci_identity_policy.cpp
      - path: src/core/elf.cpp
      - path: src/core/nvfw.cpp
      - path: src/core/vbios.cpp
      - path: src/core/frts.cpp
    dependencies:
      - sdk: DriverKit.framework
      - sdk: PCIDriverKit.framework
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.memphiscat.RTXMacHost.RTXMac
        PRODUCT_NAME: com.memphiscat.RTXMacHost.RTXMac
        SDKROOT: driverkit
        DRIVERKIT_DEPLOYMENT_TARGET: "25.2"
        SUPPORTED_PLATFORMS: driverkit
        ARCHS: "x86_64"
        EXCLUDED_ARCHS[sdk=driverkit*]: "arm64"
        ONLY_ACTIVE_ARCH: "NO"
        INFOPLIST_FILE: macos/Driver/Info.plist
        CODE_SIGN_ENTITLEMENTS: macos/Driver/RTXMac.entitlements
        DEAD_CODE_STRIPPING: NO
        DEFINES_MODULE: NO
        HEADER_SEARCH_PATHS:
          - $(inherited)
          - $(SRCROOT)/macos/Driver
          - $(SRCROOT)/include
        OTHER_LDFLAGS: "$(inherited) -framework DriverKit -framework PCIDriverKit"

  RTXMacHost:
    type: application
    platform: macOS
    scheme: {}
    sources:
      - path: macos/Host
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.memphiscat.RTXMacHost
        PRODUCT_NAME: RTXMacHost
        MACOSX_DEPLOYMENT_TARGET: "26.0"
        ARCHS: "x86_64"
        EXCLUDED_ARCHS: "arm64"
        INFOPLIST_FILE: macos/Host/Info.plist
        CODE_SIGN_ENTITLEMENTS: macos/Host/Host.entitlements
        ENABLE_HARDENED_RUNTIME: NO
        SYSTEM_EXTENSION_TARGETS: RTXMac
    dependencies:
      - sdk: IOKit.framework
      - target: RTXMac
        embed: true
        codeSign: false
